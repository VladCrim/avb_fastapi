# URL Shortener Service (FastAPI Training Task)

Простой HTTP-сервис на **FastAPI**, реализующий функционал сокращения URL и перенаправления по короткой ссылке. Также включает пример асинхронного запроса к внешнему API.

Сервис полностью соответствует требованиям задания:

1. Принимает URL и возвращает его сокращённую версию (`POST /` → `201 Created`).
2. По короткому идентификатору возвращает оригинальный URL через редирект (`GET /<id>` → `307 Temporary Redirect`).
3. Реализован асинхронный эндпоинт для запроса к внешнему сервису.

---

## 🚀 Возможности

- 📥 **Сокращение URL** через POST-запрос.
- 🔗 **Автоматический редирект** по короткой ссылке.
- ⚡ **Асинхронная интеграция** с внешним API (пример).
- ✅ Валидация входящих данных с помощью Pydantic.

---

## 🛠 Технологии

- **Python 3.10+**
- **FastAPI** — современный веб-фреймворк для создания API.
- **Uvicorn** — ASGI-сервер для запуска приложения.
- **aiohttp** — для асинхронных HTTP-запросов (внешний API).
- Временное хранилище URL в памяти (`dict`) — для демонстрации (в продакшене заменяется на БД).

---

## 📦 Установка и запуск

1. **Клонируйте репозиторий** (если применимо):
   ```bash
   git clone <ваш-репозиторий>
   cd AVB-FastApi